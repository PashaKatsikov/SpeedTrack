<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Solid brand-dark colour behind Flutter until the loading screen
         draws its first frame. Prevents the white window flash on cold
         start (§16 of the gray-flow pitfalls guide). -->
    <item android:drawable="@color/launch_background" />
</layer-list>
